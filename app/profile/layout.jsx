export const metadata = {
  title: "My Account",
  description: "Manage your EliteCrew profile, saved addresses, and settings.",
  robots: { index: false, follow: false },
};

export default function ProfileLayout({ children }) {
  return <>{children}</>;
}
